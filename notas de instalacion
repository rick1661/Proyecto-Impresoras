//Comando para crear entorno node JS, correr en consola "npm init -y"
//Instalar los siguientes modulos:
//npm i express
//npm i express mssql
//npm i express mssql cors
//npm i express mssql cors dotenv
//npm i express mssql cors morgan
//npm i nodemon -D 
//npm install dotenv
//npm install select2
//Correr el server  npm run dev
//sudo npm install -g nodemon
//npm install jquery

//comando para ejecutar docker mssql docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=<password>" -p 1433:1433 -d mcr.microsoft.com/mssql/server:2022-latest


sudo apt update
sudo apt install git


git config --global user.name "Tu Nombre"
git config --global user.email "tu@email.com"

ssh-keygen -t ed25519 -C "tu@email.com"


cat ~/.ssh/id_ed25519.pub

probar la conexion de la llave en el equipo

 ssh -T git@github.com

 ----
 contenedor docker mssql : 2bfa99364046 
 

 -Instalacion de en el servidor

 npm install nodejs
 npm install pm2 -g


 --Confugar el servicio con pm2

 pm2 start archivoruta --name archivonombre

 pm2 start src --name app.js // este 

 ver los servicios 

 pm2 list

 configurar pm2 para que soporte los reinicios

 pm2 startup
 pm2 save

 npm install net-snmp
