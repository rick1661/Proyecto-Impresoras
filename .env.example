# Configuración de Base de Datos
# Copiar este archivo como .env y completar con los valores reales

DB_USER=tu_usuario_sql_server
DB_PASSWORD=tu_password_seguro
DB_HOST=tu_servidor\instancia
DB_NAME=ImpresorasBD

# Configuración del servidor
PORT=5500
NODE_ENV=development

# Configuración de certificados SSL
# IMPORTANTE: Mantener certificados FUERA del repositorio
SSL_KEY_PATH=/ruta/externa/certificados/key.pem
SSL_CERT_PATH=/ruta/externa/certificados/cert.pem

# Ejemplos por entorno:
# Desarrollo: /secure-certs/development/key.pem
# Producción: /etc/ssl/private/app-key.pem

# URLs del frontend (opcional)
FRONTEND_URL=https://tu-dominio:5500

# Configuración CORS para entorno hospitalario (ambas VLANs)
CORS_ORIGINS=https://simnsa.local,https://192.168.85.1,https://192.168.80.1,https://localhost:5501
# VLAN 192.168.85.x: Red de equipos médicos e impresoras
# VLAN 192.168.80.x: Red administrativa y estaciones de trabajo
# Agregar todas las IPs/dominios específicos que necesiten acceso

# Configuración de seguridad
# Headers de seguridad
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self' cdn.jsdelivr.net cdnjs.cloudflare.com code.jquery.com
CSP_STYLE_SRC='self' 'unsafe-inline' fonts.googleapis.com cdnjs.cloudflare.com
CSP_FONT_SRC='self' fonts.gstatic.com
CSP_CONNECT_SRC='self' https: http: ws: wss:

# Rate limiting para entorno hospitalario (valores muy permisivos)
# 🏥 Configuración optimizada para uso intensivo en hospital
RATE_LIMIT_GENERAL_WINDOW=900000  # 15 minutos
RATE_LIMIT_GENERAL_MAX=50000      # 50,000 requests/15min (~55 req/seg promedio)
RATE_LIMIT_TONER_WINDOW=600000    # 10 minutos  
RATE_LIMIT_TONER_MAX=2000         # 2,000 requests/10min (~3.3 req/seg)
RATE_LIMIT_ADMIN_WINDOW=900000    # 15 minutos
RATE_LIMIT_ADMIN_MAX=5000         # 5,000 requests/15min para operaciones admin

# NOTA: Los límites incluyen bypass automático para:
# - Entorno de desarrollo (NODE_ENV=development)
# - IPs locales (127.0.0.1, ::1)  
# - VLAN médica (192.168.85.x)
# - VLAN administrativa (192.168.80.x)

# Configuración de logging
LOG_LEVEL=info
LOG_FILE=logs/app.log